# Copy to .env and fill in your own values. Never commit .env.
# Every variable the application can read is listed here, with no real
# value: this file is how the next person finds out a variable exists.

# Walking route provider (US-107). Not chosen yet.
GO_MEAL_ROUTING_API_KEY=

# Production only — set as host secrets, not in a file.
# RAILS_MASTER_KEY=
# DATABASE_URL=
